# Screenshot Placeholder

Screenshot capture is not feasible in this headless environment.

Manual deterministic action:

1. Run the stack per [doc/RUNBOOK.md](../RUNBOOK.md).
2. Sign in with a user that has at least one live broker connection.
3. Open Dashboard after refresh shows non-zero holdings and source status `ok`.
4. Capture the page and save as `doc/screenshots/post-mvp-dashboard.png`.
5. Update [README.md](../../README.md) to embed that file once committed.
