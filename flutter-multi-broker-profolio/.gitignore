# Secrets — NEVER commit
conn_key.pem
infra/futu-opend/conn_key.pem
*firebase-adminsdk*.json
*service-account*.json
*-credentials.json
.env
.env.*
!.env.example
.secrets/
**/.secrets/
*.key
!**/firestore.rules

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.iml

# Flutter / Dart (will be needed once flutter-bootstrap runs)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
coverage/

# Python (will be needed once backend-bootstrap runs)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Logs
*.log

# Local backup staging (off-site copy is the real backup)
infra/ghostfolio/backups/

# Own on-chain addresses — deanonymising, keep in the private config repo (§4.5)
config/own_accounts.yaml

# Raw Binance responses archived before normalisation (§5.5)
data/binance_raw/
